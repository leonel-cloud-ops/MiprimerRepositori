def transformar_lista(lista_palabras, opcion):
    lista_transformada = []
    for palabra in lista_palabras:
        lista_transformada.append("transformar_texto"(palabra, opcion))
    return lista_transformada
